# 本地环境配置模板：复制为 application-local.yml 并填入真实值
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tech_blog?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: your_db_user
    password: your_db_password
  data:
    redis:
      password: your_redis_password
